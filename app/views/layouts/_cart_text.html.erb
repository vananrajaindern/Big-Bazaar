<% @allorderscount=ProductOrder.where(shopping_cart_id: session[:cartid]).map(&:order).select{ |f| f.orderstatus !='Paid' } %>

<%= link_to "#{@allorderscount.count} Items in Cart", shoppingcart_path(session[:cartid]), class: "btn btn-link" %>
